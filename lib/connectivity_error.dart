import 'package:flutter/material.dart';

class InternetError extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Internet Connectivity Error"),
    );
  }
}
